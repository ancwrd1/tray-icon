---
"tray-icon": minor
---

On the Linux platform with `ksni` feature, add a public API to set the tray icon name from the current desktop icon theme.
If the icon name is unknown, the pixmap icon will be used as a fallback.
This also enables the automatic dark/light icon theme switch on Linux.
